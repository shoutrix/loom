# Loom

The unification refactor is tracked in `PLAN.md` at the repo root.

**Resume protocol:** read `PLAN.md`, find the first unchecked phase, execute its Steps, run its Verification, flip the checkbox, commit, append a Session log line.

Working branch: `refactor/knowledge-substrate`.
